databases:
  - name: tolyx-db
    plan: free
    databaseName: tolyx
    user: tolyx

services:
  - type: web
    name: tolyx-app
    runtime: node
    plan: free
    buildCommand: npm install -g pnpm@10 && npm run build
    startCommand: npm start
    healthCheckPath: /api/healthz
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: SESSION_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: tolyx-db
          property: connectionString
      # Set these manually in the Render dashboard after first deploy:
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      # Set to your Render app URL, e.g. https://tolyx-app.onrender.com
      - key: APP_URL
        sync: false
