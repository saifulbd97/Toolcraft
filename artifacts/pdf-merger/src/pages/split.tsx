import ToolStub from "./tool-stub";

export default function Split() {
  return (
    <ToolStub
      title="Split PDF"
      description="Separate a PDF into individual pages or extract a range of pages."
    />
  );
}
