import ToolStub from "./tool-stub";

export default function Compress() {
  return (
    <ToolStub
      title="Compress PDF"
      description="Reduce PDF file size without sacrificing quality — perfect for sharing."
    />
  );
}
